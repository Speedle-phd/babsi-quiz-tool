import type { QuestionTypeProps } from '@/types/types'


const SingleChoice = ({ options, answer, points }: QuestionTypeProps) => {
   console.log(options, answer, points)
   return <div></div>
}

export default SingleChoice
